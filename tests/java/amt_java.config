[ArachneMergeTool]
    verbose = false
    tools = mac;mji

[mergetool "mji"]
    order=android

[mergetool "mac"]
    order=localfirst
    report=unsolved

[mergetool "meld"]
    cmd = meld --diff $LOCAL $BASE --diff $BASE $REMOTE --diff $LOCAL $MERGED $REMOTE

[mergetool "kdiff"]
    path = /usr/bin/kdiff

