[ArachneMergeTool]
    verbose = false
    tools = mji

[mergetool "mji"]
    order=android

[mergetool "meld"]
    cmd = meld --diff $LOCAL $BASE --diff $BASE $REMOTE --diff $LOCAL $MERGED $REMOTE

[mergetool "kdiff"]
    path = /usr/bin/kdiff
